# Server-side destination used by the protected Next.js proxy.
SWARM_CONTROL_PLANE_URL=http://127.0.0.1:8000
# Must match the backend SWARM_API_KEY when Bearer authentication is enabled.
# Both values remain server-side; never prefix them with NEXT_PUBLIC_.
SWARM_CONTROL_PLANE_API_KEY=
